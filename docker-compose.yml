version: '3.9'

services:
  web:
    build: .
    command: gunicorn carrie_search.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - carrie_search/.env
